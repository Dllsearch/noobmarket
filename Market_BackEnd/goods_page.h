#pragma once

#include <string>
#include <vector>

/*

class goods_page
{
private:

	enum DiscountType;

	float basePrice;
	DiscountType currentDiscountType;
	float discountPrice;
	float discountPC;
	float priceCalculated;
	
	void recalculatePrice();
	
public:

	float getPrice();

	void setBasePrice(float price);
	
	void setDiscointPrice(float discountPrice);

	void setDiscointPC(float discountPC);

	std::string generateCouponPC(float discountPC);

	std::string generateCouponDiscount(float discount);

};

*/